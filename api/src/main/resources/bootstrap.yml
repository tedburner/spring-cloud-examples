spring:
  application:
    name: example
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        service-id: config-server
        enabled: true

## 服务注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
server:
  port: 9000